修订动作（基于本次 review，仅在构思层面落地，不越界到写作）：

1. 针对 I-1：在“事实缺口”第 1 条之外，把“是否绑定一个真实脚本作为主线例子”从隐含选项升格为写作阶段的硬前置——也就是说，进入写作模式之前，必须先从作者那里拿到这个决定；如果作者明确不绑定真实脚本，则本文应自动降格为更短的“判断结构小品”，而不是按当前六节结构写满，否则容易触发技术实践文禁区。这条已被本次构思显式承认，无需改大纲，但需要在交给写作阶段时把这一前置作为入参传下去。
2. 针对 I-2：在第 4 节四个维度的写作约束里追加一条硬要求——每个维度末尾必须有一句回钉到核心判断的短句（句式留给写作阶段，不在构思里写死，避免提前固化措辞）。这条作为写作阶段的进入条件，构思大纲不变。
3. 针对 M-1、M-2：留作写作阶段处理，不在构思层面强行落地措辞，避免把构思写成半成品正文。

不需要重写大纲的原因：本次 review 没有 C 级问题，且 I 级问题都可以通过“向写作阶段传递硬前置 + 局部约束”解决，没有触发结构性返工。如果作者在写作前对“事实缺口”第 1、2、3 条任何一条给出实质性补充，构思应再过一遍，把抽象判断替换为基于真实材料的判断，那才是真正会改变大纲的修订点。

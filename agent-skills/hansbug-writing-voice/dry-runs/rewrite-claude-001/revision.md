按上面自审做最小修订，原则是“判断主线不动，只补边界、加一处落锤、加一处对立拆分”：

1. 针对 I-边界：在第一段末追加一句——“顺带先把评价边界划清：本文只评原文这段说明书的写法本身；要评工具本体好不好用，得另外有作者真实使用环境，这里不替它脑补。”
2. 针对 M-节奏：把第三段末尾“前者像在炫菜单，后者才是在划边界”后追加一句短句落锤：“路径只是地址，覆盖才是决策。”
3. 针对 M-拆分：把第五段“它等价于‘请自己看着办’”改写为“它不是建议，而是把决策成本原样退回给用户——披着‘指引’外皮的甩锅。”

未做更大幅度改动的原因：原文极短，事实点稀少，再加内容会越界进“替作者脑补工具能力”的 C 级风险区；自审里 C=0，骨架已经站住，剩下的只是节奏和拆分层面的微调，按 `review-rubric.md` 属于 M 级、个别 I 级，不需要返工重写。机械 gate（`scripts/check_hansbug_voice.py`）本次未实际执行，因为这是 dry-run forward-test、不写入仓库文件；如需正式入库为博客正文，应在落盘后按 SKILL.md 的命令补跑一次 JSON 输出，并复核 `blockingFindings` 为空。

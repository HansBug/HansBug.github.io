C=0 / I=0 / M=2

M-1：未交代“依赖”具体指哪条生态（pip / npm / apt 等）。本次任务输入未提供该信息，按硬要求保持事实背景不变，未编造。需要作者补充。
M-2：未给出 `A` / `B` / `C` 的真实参数名（如 `--input` / `--output` / `--force`）。原文用占位符，未扩写。需要作者补充。

入口确认：已遵守 `CLAUDE.md` 的 "HansBug 文风 Skill 强入口"，并按 `agent-skills/hansbug-writing-voice/SKILL.md` 任务模式表执行 rewrite 路径。

本任务是 `审阅`，不要求我直接重写候选稿，因此我没有对候选稿做行级改写——按 SKILL.md "审阅时，把文风问题落到可执行修改或具体例子上"的要求，我已经把每一条 C/I/M 都给出了具体证据位置和修复方向。

我在本轮自审中复核了下面几件事：

- **finding 是否都能落到证据上**：每条 C/I 都引用了候选稿原文片段或具体章节，而不是"感觉不像"。M 级也都指向了具体标题或具体节奏问题。符合 SKILL.md 工作流第 8 条。
- **是否区分了文风问题和事实来源问题**：是。事实来源轴单独走了一次评分（Pass，但说明了 Pass 的原因是低信息量而非处理得当），没有和文风轴混在一起打 C。
- **是否避免了空话**：我没有写"感觉不像""味道不够"这种 rubric 明确点名禁止的措辞，结论都落到"删什么 / 补什么 / 重排成什么骨架"上。
- **是否避免了二次违规**：审稿稿本身也没有编造作者经历，没有把 holdout / negative 样本当正向证据，没有把机械 gate `score` 误读为文风相似度。
- **是否需要补跑 `check_hansbug_voice.py`**：按 SKILL.md，宣称"通过文风 gate"才必须跑脚本；本任务是审阅一份外部候选稿且本人结论已经是 C=3 阻断，不存在"通过 gate"的宣称，因此不补跑脚本结果。若 reviewer 后续希望补一份机械粗筛证据，可对该候选稿单独执行该脚本——但即使脚本意外通过，也不改变本审稿的 C 级阻断结论（脚本通过不等于像）。

如果后续要求我把这份审稿"升级"成直接改写候选稿，那将进入 `改写` 模式而非 `审阅` 模式，需要重新按 SKILL.md 任务模式表加载 `voice-profile.md` 和 `micro-patterns.md`，再按 rubric 重建一个带核心判断的最小骨架。当前仅按 `审阅` 模式输出问题清单与修法。

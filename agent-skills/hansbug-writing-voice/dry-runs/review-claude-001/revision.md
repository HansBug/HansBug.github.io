本次 dry-run 自审结果为 C=0 / I=0，无阻断性问题需要修订，因此不对 `## draft` 主体进行内容增删。

仅做以下非阻断说明：
- 我没有把 `check_hansbug_voice.py` 的实际运行结果贴进来，因为本任务是“审阅他人候选稿”，而不是产出可被脚本检查的 HansBug 正文片段；脚本运行属于 `write` / `rewrite` / `fix-ai-cliche` 类任务的硬要求，与本任务的 review 模式不冲突。
- 我没有给候选稿写“修复后的正文示例”，因为：(a) 缺少 C1 / C2 / C3 所需的一手事实，agent 编造会违反硬性要求；(b) 任务类型是 review，不是 rewrite，主体输出应是 C/I/M 判定与修法指南，不应越过任务边界生成新正文。
- 关于 M-self-2 的精简版本：因 PR-5 验收明确要求按本任务模板写完整的三段式输出，不在本轮压缩；如后续需要短版，再由 rewrite 任务承接。

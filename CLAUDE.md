# CLAUDE.md / AGENTS.md

> 注意：`CLAUDE.md` 与 `AGENTS.md` 是同一个文件；`AGENTS.md` 只是指向 `CLAUDE.md` 的软链接。请只维护这一份内容，不要重复编辑或分别修改。

本文件用于未来继续维护本站的 agent / 协作者快速上手。

和 `README.md` 相比，这里更偏“维护约束 + 实际工作路径 + 不要做什么”。

## 一句话概述

这是 HansBug 的中文个人技术博客仓库，当前运行在：

- 仓库：`HansBug/HansBug.github.io`
- 线上：`https://hansbug.github.io`

站点定位不是个人介绍页，而是长期维护的中文技术写作与知识索引工作台。

## 维护优先级

1. 发布链路稳定
2. 新文章进入路径清晰
3. 标签、路线、归档等索引持续可用
4. 联系入口始终明确
5. 视觉克制、简洁、淡雅

## 当前架构摘要

### 主体栈

- Astro
- Vue
- TypeScript
- Chart.js
- GitHub Pages
- GitHub Actions

### 关键路径

- 站点配置：`src/config/site.ts`
- 内容 schema：`src/content.config.ts`
- 内容聚合逻辑：`src/utils/content.ts`
- 全局样式：`src/styles/global.css`
- GitHub Pages 工作流：`.github/workflows/deploy.yml`

### 旧站归档页

`old-blog` 现在不是纯 Astro 静态片段，而是：

- 页面入口：`src/pages/old-blog.astro`
- 主交互组件：`src/components/OldBlogExplorer.vue`
- 下拉筛选组件：`src/components/OldBlogFilterSelect.vue`

如果任务涉及 `old-blog` 的筛选、排序、趋势图或交互，优先改 Vue 组件，不要重新回退到：

- 原生浏览器默认 `select`
- 零散的内联 DOM 拼接
- 大段无类型的脚本状态管理

## 内容系统约定

### 博客文章

目录：`src/content/blog/`

schema 以 `src/content.config.ts` 为准，当前核心字段：

- `title`
- `description`
- `pubDate`
- `updatedDate`
- `tags`
- `difficulty`
- `excerpt`
- `series`
- `draft`
- `featured`
- `pinned`
- `routeSlugs`

行为规则：

- `draft: true` 不会被发布
- 排序规则是 `pinned` 优先，然后按 `pubDate` 倒序
- 文章详情页路由来自内容文件路径本身
- `routeSlugs` 用于把文章显式挂到路线

### 阅读路线

目录：`src/content/routes/`

行为规则：

- 路线按 `order` 升序
- 如果配置了 `recommendedPosts`，路线页优先使用它
- 否则回退到 `routeSlugs + 共享标签`

### 项目页

目录：`src/content/projects/`

行为规则：

- 项目按 `featured` 优先，再按 `order`

### 标签

- 标签元数据集中维护在 `src/data/tagMeta.ts`
- 新标签优先复用现有标签
- 如必须新增，必须补标签分组、描述和配色

## 新文章发布流程

### 推荐步骤

1. 在 `src/content/blog/` 下新建 Markdown 文件
2. 填完整 frontmatter
3. 优先复用现有标签；必要时补 `src/data/tagMeta.ts`
4. 如果文章属于某条路线，补 `routeSlugs`
5. 执行 `npm run build`
6. 本地检查首页、博客页、文章详情页、标签页
7. 推送到 `main`

### 路径建议

推荐使用稳定、可长期维护的路径，如：

- `src/content/blog/python/treevalue-overview.md`
- `src/content/blog/engineering/github-actions-notes.md`

不要把路径命名成临时口语化、难以长期维护的 slug。

## 博客写作风格约定

这部分约束的是 `src/content/blog/` 下的 博客正文 写法，而不是首页、导航、按钮或普通页面说明文案。

博客正文默认应保留明确的作者视角与判断，不必压成“完全中性、完全说明书化”的口气。后续新文章，默认应向 HansBug 在旧博客园 `2018` 年及之后文章里已经稳定形成的写法靠拢，重点不是机械复刻个别口头禅，而是延续那种 一看就知道是他本人在写 的行文重心、分析方式和语气张力。

- 参照样本：
  - 后续判断“像不像 HansBug 的文章”，优先参考这批旧文：
    - `2018`：`【技巧】Java工程中的Debug信息分级输出接口及部署模式`
    - `2018`：`【作业】HansBug的前三次OO作业分析与小结`
    - `2018`：`【作业2.0】HansBug的5-7次OO作业分析与小结，以及一些个人体会`
    - `2020`：`【学习笔记】Latex各平台2020实战攻略`
    - `2020`：`【开源系列】项目开源实战记录-序`
    - `2021`：`【助教工作】2021团队项目助教跟班全攻略`
    - `2021`：`【项目管理】关于Issue/Milestone的使用指导`
    - `2021`：`沉舟侧畔千帆过，病树前头万木春——对【题士】产品的深度测评与解析`
    - `2021`：`姗姗来迟的一个总结`
    - `2022`：`treevalue——Master Nested Data Like Tensor`
  - 这些文章题材差异很大，但有几条共同的“作者签名”，后续写新文章时应主动保留。

- 总体气质：
  - 文章要有强烈的“人写的”痕迹：懂技术、做过事情、踩过坑、敢下判断，也愿意把判断背后的理由讲透。
  - 语气允许带一点松弛、打趣、自嘲、插科打诨，但主体仍然是严肃分析，不是玩梗文，不是段子文。
  - 文章不能写成冷冰冰的资料搬运，也不能写成只有情绪没有结构的长段吐槽；HansBug 的辨识度恰恰在于技术判断很硬，表达却不板。
  - 不能写得过于“工整”“洁净”“像被修平了毛边”。原文经常保留轻微的即兴感、现场感、插播感，像是作者正在一边讲、一边想、一边顺手把结论钉进去，而不是把整篇 prose 修成一块过分圆滑的石头。
  - 对外要坦率，甚至偶尔锋利；但锋利必须建立在事实、经验、推理和案例之上，不能空发脾气。
  - 风格里应允许保留一定的棱角、辛辣感和黑色幽默感。很多旧文并不是温吞地“提出一点建议”，而是会在关键处用带刺的表达把问题钉住，让读者明确感受到作者对糟糕设计、偷懒方案、虚假勤奋、空洞流程的反感。
  - 这种锋利感不是粗暴开喷，而是先把问题讲明白，再用一句略带戏谑或毒辣的判断把它钉死。换句话说，刀子必须有刀背，不能只有刀刃。

- 最有个人辨识度的宏观结构：
  - 开头通常先用一小段“人话”破题：近况、缘起、题外话、自嘲、吐槽、补坑说明，都可以。
  - 这种开场不能拖太久；通常 `1-3` 段内就要收回正题，明确本文到底为什么写、想解决什么问题。
  - 破题之后，尽快交代文章边界：基于什么场景、针对什么版本、哪些内容会讲、哪些内容暂时不讲。
  - 文章不是写到最后才知道结论，而是通常会比较早就亮出判断，后文主要负责把这个判断论证扎实。
  - 正文一定要有明显层次，优先采用 `概述 / 背景 / 前言 -> 主体分析 -> 总结 / 反思 / 展望` 的大结构，且中间几节要形成递进，不要只是并列堆料。
  - 结尾不能只说“感谢阅读”或者“先写到这里”；必须收束回自己的最终判断，让读者知道作者到底怎么看这件事，并且最好把视角再抬高半层。
  - 最像原文的推进轨道通常是：
    - `缘起 / 题外话破题`
    - `问题定义 / 边界交代`
    - `结论或核心判断前置`
    - `分维度展开分析`
    - `补坑点、反例、边界或更深层原因`
    - `总结与上升一层的反思`
  - 这种结构最关键的不是“分几节”，而是整篇始终有一条判断主线：
    - 从现象走到本质。
    - 从局部实现走到工程取舍。
    - 从一次事故走到链路设计。
    - 从一次体验走到产品思路。
    - 从一段经历走到制度、方法或价值判断。

- 开头、章节和结尾应该怎么写：
  - 开头可以先热一下场，但热场必须服务于后面的分析，而不是纯抒情。
  - 允许出现带作者个人温度的开场语气，例如交代“这文为什么拖到现在才写”“最近被什么事情逼得不得不写”“如果只想看干货可以直接跳某节”。
  - 更像原文的开头，往往不是“背景介绍”四个字就算完，而是先把作者本人放进场景里：最近被什么问题追着打、哪件事把自己逼到不得不写、这篇为什么拖到现在、自己对这件事现在是什么态度。
  - 常见做法是先用半口语方式把读者拉进来，然后立刻切到“说正经的”“闲话先收一收”“本文真正要讲的是……”这种收束动作。
  - 章节标题最好不仅告诉读者“这一节讲什么”，还顺手暗示“作者打算怎么讲、准备得出什么判断”，例如：
    - `为什么单靠标签不够`
    - `我最后采用的结构`
    - `为什么这种问题特别烦`
  - 倒数第二节往往不是简单补遗，而是用来处理“真正难的地方”，例如补坑点、例外、制度层原因、方法论反思。
  - 最后一节很重要，不能只是重复前文，而要落一个更完整的判断；必要时可以带一点宣言感、诗性、口号感或价值抬升，但前提是前文已经把铺垫做够。
  - 原文结尾常常不是“收住”，而是“盖章”。也就是说，最后几句最好有明显的落锤感，像是作者把整篇文章的精神内核钉在墙上，而不是温吞地把材料回收到一个结论框里。
  - 允许但非必须的开场或结尾装置：
    - `UPDATE`
    - 引语、诗句、名言
    - “太长不看版”
    - “闲话少叙，让我们开始吧”
  - 这些装置是调度节奏的工具，不是每篇都必须照搬的摆设。
  - 如果是技术实践文、工具配置文、环境搭建文，且文章本身可以直接指导读者把问题交给 Codex / Claude / 其他本地 agent 处理，开头可以在正式破题前放一个“三段式 quick-start 引用块”。这不是每篇都必须有，但一旦使用，除文章内容本体相关变量外，其余句式尽量固定下来，形成本站自己的开头风格。
    - 第一段必须是独立的 `>` 引用块，固定写法为：`本文大体是在讲[本文包含什么]。它解决的是“[解决的问题]”的问题，不解决[不解决的边界]的问题。` 变量只能落在方括号对应内容上；变量可以是完整短语或并列短语，但不要重组句子骨架，不要用“笔者建议先把话说在前面”这种生硬套话开场。中文正文里的引号统一用 `“”`，不要写成 ASCII 直引号。
    - 第二段必须是另一个独立的 `>` 引用块。加粗引导句默认固定写法为：`**如果你只是需要解决“[更短的问题描述]”这件事，那你该做的事情不是继续往下慢慢读，而是直接把下面这段无脑复制过去丢给你亲爱的无敌的 Codex / Claude Code：**` 如果目标不是 Codex / Claude Code，可以只替换最后的 agent 名称，其余句式不改。
    - 第二段里的 agent 指令必须用独立 fenced code block 放出，语言必须写 `text`。这不是单纯的视觉偏好，而是为了避免语法高亮器把 `{{PAGE_URL}}` 拆成多个 token，导致运行时替换变得脆弱。默认第一句固定写成：`请阅读这个页面：{{PAGE_URL}}，[按本文方案完成的具体任务]`；其中 `{{PAGE_URL}}` 必须保持在源 Markdown 的同一物理行内，不要折成 `{{` / `PAGE_URL` / `}}` 多行。后面继续补任务细节和安全边界。
    - 第二段 code block 后必须固定补一句：`**让它给你搞定就好，比你自己一边翻全文一边手搓配置高效得多。**`
    - 第三段必须是第三个独立的 `>` 引用块，固定写法为：`如果你关心[方案取舍 / 边界 / 坑点 / 原理 / 长期维护判断]，那才建议继续往下读。` 方括号内只填继续阅读的理由，不要改“如果你关心……那才建议继续往下读”这个句式。
    - 这三段必须在 Markdown 里真正断开，生成三个独立 `blockquote`，不能只在同一个引用块里用 `>` 空行分段；否则视觉上会糊成一整块，读者抓不住重点。
    - 第二段的 agent 提示词必须保留必要安全边界：不要保存真实凭据、不要自动执行高风险动作、遇到环境或参数不一致时先问用户。能直接复制，不代表可以把权限边界也一起省掉。

- 锋利感、戏谑感与黑色幽默：
  - 允许在合适场景下写得更尖锐一点，尤其是复盘、测评、课程反思、流程批评这类文章里。
  - 可以有一点黑色幽默，甚至一点不太客气的戏谑感，但必须建立在分析已经站稳的前提下。更像 HansBug 的写法通常是：前文把逻辑铺够，后文用一句带刺的话把问题一锤定音。
  - 这种戏谑往往不是为了逗乐，而是为了制造反差，让读者感受到“这件事表面上似乎还行，实际上荒唐得很”。
  - 也就是说，可以毒，但要毒得有理；可以黑色幽默，但要幽默在判断之后，而不是替判断本身充数。
  - 可以适度保留那种“说白了”“别的不说”“请各位自己想想看”“好吧，这事其实不复杂”之类的掰扯感与当面质问感。原文很多地方像是在和读者面对面掰开讲，这种交流感比纯说明书式陈述更像本人。
  - 适合保留的锋利方向：
    - 对自欺欺人的流程提出嘲讽。
    - 对“看起来很努力，实际全在做无效功”的状态提出讥讽。
    - 对表面漂亮、底层站不住的设计做冷处理式评价。
    - 对“能跑就行”“差不多就行”“以后再说”的短视做明确否定。
  - 不适合保留的方向：
    - 纯人身攻击。
    - 没有事实支撑的阴阳怪气。
    - 只有态度没有分析的情绪宣泄。
    - 为了显得锋利而刻意句句夹枪带棒。

- 行文推进与概念拆分：
  - 很多段落不是“平铺信息”，而是“先下判断，再解释为什么，再补证据、案例或改法”。
  - 文章里常出现“先立评价坐标，再做判断”的动作。例如产品测评不会一上来就开喷，而是先说明用什么标准看；教程文不会一上来贴命令，而是先讲问题背景、适用场景和整体定位。
  - 很常见的一种辨析动作是：把两个看似相近的概念硬生生掰开讲清楚。例如“标签”和“路线”不是一回事，“失败点”和“原因点”不是一回事，“看起来好用”和“长期可维护”也不是一回事。
  - 这种辨析不是为了显得会说话，而是为了把问题的判断坐标钉住。HansBug 的很多文章之所以读起来有“这个人很有自己一套”的感觉，核心就在于：他往往不会顺着现成说法滑过去，而是先把概念重新定义一遍，再开始分析。
  - 适合多用设问、反问和“那么问题来了”式的抛转。原文经常会先把读者心里的偷懒想法说出来，再亲手把它拆掉，这种先代言、再反杀的动作很有个人辨识度。
  - 常见推进顺序：
    - 先说表层现象是什么。
    - 再说本质问题其实是什么。
    - 再说如果不改会怎样。
    - 最后给一个可操作的改法或更优思路。
  - 高频判断句法：
    - 不是 `A`，而是 `B`
    - 看起来是 `X`，本质上是 `Y`
    - 表面问题是 `P`，根因其实是 `Q`
    - 与其说是 `M`，不如说是 `N`
  - 很像原文的一点，是愿意把一个问题拆到“坐标系”层面，而不是顺着大家现成的话术往下滑。

- 段落节奏、断句与修辞：
  - 旧文很少一上来就是大段平铺说明，更常见的是：先用一句有判断的句子把段落立住，再往后补理由、案例和延伸。
  - 段落长度通常中等偏短，一段只做一件事；当判断转向时，宁可另起一段，也不要在同一段里混太多层意思。
  - 常见节奏是“长句交代背景，短句落结论”。可以先把上下文讲清楚，但最后最好用一句更短、更硬的句子收一下，让读者知道这一段到底想说什么。
  - 可以适量使用破折号、分号、括号补充来制造停顿感和解释感，但不要把一句话写得失控。
  - 可以保留较强的比喻感和画面感，尤其在批评、总结、拔高时。HansBug 的原文里经常会把抽象问题说成一种非常具体、甚至略带夸张的处境，这种写法是其个人辨识度的重要来源之一。
  - 比喻不必总是文雅。原文有时会故意把问题说得有点狼狈、有点荒唐、有点黑色幽默，这种“画面不一定好看，但一下就记住了”的比喻方式，应允许保留。
  - 关键判断可以通过独立成句、加粗、设问、反问或一句更硬的短句来强化，但不要为了制造气势而机械重复整段。
  - 不要把每个段落都修成差不多的长度和力道。HansBug 的原文经常会在某一段突然拉长、突然提高情绪、突然补一个很毒的类比，然后再收回来；这种摆幅是辨识度的一部分。

- 语气、人称与用语习惯：
  - 默认允许并鼓励使用 `笔者` 这一自称；这是旧文里非常稳定的个人标记。
  - 在情绪更强、距离更近、需要更直接的时候，可以切到 `我`，但不要整篇在 `笔者` 和 `我` 之间来回乱跳。
  - 允许少量括号补充、顺手插一句、轻微自嘲、偶发的 `2333` 式语气；但它们只能是调味料，不能把整篇文章写成模仿腔。
  - 允许偶尔带一点古典化、修辞化、戏剧化表达，尤其在标题、开头、总结处，但正文分析阶段始终要把清晰度放在前面。
  - 允许作者偶尔直接对读者说话，例如“请问各位”“不妨想一想”“别的不说”“好吧”，这种距离突然拉近的动作很像原文。
  - 可保留的转场词和语气词：
    - `首先`
    - `其次`
    - `具体来说`
    - `另一方面`
    - `换句话说`
    - `总而言之`
    - `归根结底`
    - `好吧`
    - `咳咳`
    - `容我先……`
    - `闲话少叙`
  - 这些表达只能点到即止。重点是形成一种“人在边讲边想边判断”的推进感，而不是把整篇写成口头禅拼贴。

- 经验与自我出场的方式：
  - HansBug 的文章经常带有明确的个人经历，但这些经历不是为了写履历，更不是为了自夸，而是为了交代“我为什么有资格在这里下这个判断”。
  - 如果文章需要交代自己的背景，重点应落在经验如何影响判断，而不是简单堆砌“做过什么”。
  - 更像原文的写法：
    - 先交代自己为什么会碰上这个问题。
    - 再说明自己在这个问题上踩过什么坑、做过什么事、见过什么现象。
    - 最后把这些经历收束为一个更高层的判断。
  - 不太像原文的写法：
    - 上来先铺一大段履历，然后正文和这些履历几乎没有关系。
  - 很多旧文会把“我经历过什么”转成“所以我为什么现在不再接受某种糊弄式做法”，这种由经历导向判断的转折要保留。

- 技术文里的作者声音应该落在哪：
  - 技术文章里最像 HansBug 的地方，不是口头禅，而是会下工程判断。
  - 不能只说“怎么做”，还要说“为什么这样做更合理”“什么做法看起来省事其实是在偷懒”“这一设计在长期维护上是否站得住”。
  - 很多时候，文章的亮点不在代码本身，而在于作者会把一个实现选择上升为工程取舍：为什么不用另外一种方案，为什么某个自动化做法只是看着聪明，为什么某个配置虽然能跑但不值得长期保留。
  - 如果正文里只有命令和代码，没有取舍、边界、坑点和判断，那么就算信息是对的，也还是不够像。
  - 技术文章不是纯 API 手册，也不是只贴命令不解释缘由的速查表；即便是教程，也最好带有“个人经验压缩”的感觉，而不是把官方文档重新排版一遍。

- 复盘、测评、反思文里的作者声音应该落在哪：
  - 这类文章不能停在“发生了什么”，而要继续追问“为什么会这样”“真正的问题在哪里”“谁会被这个问题伤到”“如果不改，后果是什么”。
  - 与很多泛复盘文章不同，HansBug 的文字通常不会满足于做一个温吞的事故记录；它更像是在借一件事，把背后的方法论、制度问题、设计失误或者思维误区一层层拎出来。
  - 如果文章里涉及批评，最好遵循这个顺序：
    - 先指出具体现象。
    - 再说明为什么这不是小问题。
    - 再解释这个问题背后暴露出的更深层判断失误。
    - 最后给出能落地的改法或至少给出明确方向。
  - 评价他人的作品、课程或项目时，可以直言不讳，但不要偷懒；批评必须比表扬更具体。
  - 很多旧文都会同时站在用户、开发者、PM、维护者、课程参与者等多个角度看同一件事。后续新文如果涉及评测、复盘、课程反思，也应尽量保留这种多视角拆解能力。

- 读者引导与作者判断的平衡：
  - 文章里可以出现“如果你只想看结论，可以直接跳到某节”“如果对这部分背景不感兴趣，可以略过”之类的读者引导句；这类写法在旧文里很常见，而且确实有作者本人说话的味道。
  - 这类引导句不是为了做互动感本身，而是为了降低长文阅读门槛，让结构更像人在带着读者读，而不是把一堆材料直接扔给读者自己啃。
  - 一篇像 HansBug 的文章通常会同时做到：
    - 让读者知道应该怎么读。
    - 让读者知道作者到底怎么想。

- agent 快速执行提示里的 `{{PAGE_URL}}`：
  - 三段式 quick-start 的固定模板见上文“开头、章节和结尾应该怎么写”。这里仅记录 `{{PAGE_URL}}` 的运行时行为，不重复定义文案模板。
  - 这类 prompt 推荐写在 fenced code block 里，并用 `{{PAGE_URL}}` 表示当前页面 URL。读者打开页面后，代码块里可见的 `{{PAGE_URL}}` 会被前端替换成当前浏览器页面地址；点击代码块右上角复制按钮时，复制到剪贴板里的内容也使用同一个当前页面地址。
  - `{{PAGE_URL}}` 只在代码块运行时处理，不会在构建期固定成某个 canonical URL。因此正文、inline code 或普通说明文字里直接写 `{{PAGE_URL}}`，页面上看到的仍然是这段字面量。
  - 如果需要在可复制代码块里讲解 `{{PAGE_URL}}` 这段文字本身，并且希望读者点击复制后也得到字面量 `{{PAGE_URL}}`，代码块 fence 上加 `copy-literal-page-url` meta，例如：

````markdown
```text copy-literal-page-url
{{PAGE_URL}}
```
````

  - 不要用反斜杠之类的可见字符伪装 escape，这会让读者看到和复制到多余字符；需要字面量复制时使用 `copy-literal-page-url`。

- 标题风格：
  - 标题允许保留两种高辨识度写法：
    - `【分类】主题`
    - `有文气的前半句——明确的解释性副标题`
  - 第二种写法要注意：前半句可以有文气、有情绪，但后半句必须把文章主题说清楚，不能只剩抒情。
  - 如果是系列文章，建议沿用统一命名系统，例如：
    - `系列名(0x01)——主题`
    - `系列名——主题（上篇 / 下篇）`
  - 不要求所有文章都写成旧博客园时代那种大标题，但至少要让标题既有作者味，又有检索性和可维护性。

- 不同题材可直接复用的骨架：
  - 这些只是优先骨架，不是死模板；但如果完全脱离这些结构，文章往往会迅速变得不像。
  - 技术实践文：
    - `缘起 / 这篇要解决什么 -> 先说结论 -> 为什么旧做法不够 -> 我的方案 -> 坑点与边界 -> 总结`
  - 教程 / 学习笔记文：
    - `写在前面 -> 概念或整体定位 -> 实操方案 -> 关键细节解释 -> 适用边界 / 常见坑 -> 一点总结`
  - 复盘 / 事故分析文：
    - `事故缘起 -> 先给结论 -> 表面现象 -> 根因分析 -> 为什么这类问题难查 -> 反思与改法 -> 总结`
  - 测评 / 评论文：
    - `评测范围与标准 -> 先给总体判断 -> 分维度体验 -> 问题与影响 -> 建议 -> 更高层的总结`
  - 课程 / 项目总结文：
    - `完整过程回顾 -> 各阶段判断 -> 个人总结 -> 更深一层的反思 -> 鸣谢或补充说明`

- 允许保留的个人化动作：
  - 先用一两句近况或补坑说明暖场，然后迅速切回“本文真正要解决的问题”。
  - 在开头先甩一句有点带劲的近况、抱怨或自嘲，再马上把它收回到正题，例如“最近又被这玩意追着打”“这坑拖到现在才填也算是活该”。
  - 先直接下一个判断，再用“具体来说”把原因拆开讲。
  - 先夸某个设计局部上确实舒服，再马上指出真正影响体验的缺口在哪里。
  - 先把两个容易混在一起的概念拆开，再继续往下分析，例如“这不是 X 的增强版，而是另一层完全不同的问题”。
  - 先说“这东西能跑”，然后紧跟一句“但能跑不等于设计成立”，把判断从表层成功推进到长期可维护性。
  - 在段末用一句更硬的短句收束，例如把判断压缩成一句几乎带钉子的结论。
  - 在总结部分把视角拉高一层，谈这件事背后的方法论、制度问题、价值判断或更长期的影响。
  - 在复盘文里先承认一个设计局部上确实成立，再指出它为什么仍然不够好，这样文章会更像真实判断而不是预设立场。
  - 在结尾允许补一句类似“还是那句老话”“说到底”“归根结底”“别把这事想得太轻”式的盖章句，让全文最后落在一种明显更硬的收束上。

- 不要写成这样：
  - 不要写成没有作者存在感的 AI 式说明文，通篇都是正确废话。
  - 不要写成纯流水账，今天做了什么、明天做了什么，却没有判断、没有提炼。
  - 不要为了模仿旧文而在每篇都硬塞大量俏皮口癖、表情、感叹号或 `2333`。
  - 不要只有“我觉得”，没有“为什么我这样觉得”。
  - 不要把标题写得只剩情绪和典故，导致读者看不出来文章到底讲什么。
  - 不要把旧博客园时代相对粗放的排版习惯原样搬到新站；新站应保留作者声音，但排版和信息组织要更稳、更利于长期阅读。
  - 不要为了“显得客观”而回避结论。HansBug 的文章很重要的一点，就是愿意把最后的判断明确说出来。
  - 不要只模仿表面动作。例如只学会了“咳咳”“好吧”“说正经的”，却没有学会如何立判断、拆概念、压结论，这样只会写出很假的外壳。
  - 不要把“像 HansBug”理解为“情绪更猛一点”。真正的辨识度不在情绪强度，而在于判断的组织方式、问题的拆法以及经验与分析之间的连接方式。

- 写作时自检：
  - 开头 `3` 段内，是否已经说明文章缘起与主问题。
  - 是否先交代了评价标准、适用边界或分析框架，而不是直接铺材料。
  - 是否在前半段就亮出了核心判断，而不是拖到最后才表态。
  - 每个大节之后，是否都能看到作者自己的判断，而不是只有转述。
  - 是否真的把相近概念拆开了，而不是顺着读者现成印象往下滑。
  - 锋利感是否建立在事实和推理上，而不是拿语气强度冒充洞察。
  - 口语化表达是否只是调味，而不是喧宾夺主。
  - 结尾是否比前文再高半层，而不是只做材料回收。
  - 读完整篇后，是否明显能听出“这是 HansBug 在写”，而不是任何一个泛技术博主都能替换。

## 旧博客园迁移策略

旧博客园地址：

- `https://www.cnblogs.com/HansBug/`

当前策略必须坚持：

- 不直接批量搬运旧正文
- 新站只保留入口、索引、时间、阅读量和结构化归纳
- 真正值得保留的主题，在新站重写

### 旧站数据源

旧站全量索引由脚本生成：

```bash
python3 scripts/fetch_old_blog.py
```

会更新：

- `src/data/oldBlogCatalog.json`
- `src/data/oldBlogCatalog.ts`

当前已知事实：

- 旧站共 `315` 篇文章
- 有真实阅读量、评论数、年份趋势、旧站分类
- `old-blog` 已支持趋势、筛选、检索和直接跳转博客园原文

### 改旧站归档页时不要做的事

- 不要把旧文伪装成最近新内容
- 不要把旧站怀旧模块重新塞回首页
- 不要直接复制博客园正文到新站
- 不要把旧站分类原样当成新站标签体系

## 首页和视觉约定

### 首页

- 首页只面向未来的新写作
- 只展示新文章、新入口和必要动作
- 不要再把旧站总结、迁移说明、历史归档大段挂回首页

### 视觉

- 保持轻工业感、低饱和、淡雅、克制
- 可以有轻度风格化气氛，但不要重度二次元
- 全站按钮、badge、标签、筛选器保持扁平、低厚度、轻边框
- 避免臃肿玻璃感、过高按钮、双层无意义边框
- 中文阅读舒适度优先于花哨装饰
- `HomeDeskPet` 是全站公共视觉层，默认应挂在共享布局里，不要只留在首页或只在个别页面出现

### 页面文案

- 所有展示给读者的页面文案，默认必须使用读者视角来写
- 不要在页面上出现设计者 / 维护者自述式的话，如“我为什么这样设计”“这个页面负责什么”“这里本来打算怎样演化”
- 可以写站点事实、阅读建议、内容范围和使用方式，但要写成读者进入页面后能直接理解和使用的信息
- 如果确实需要说明维护策略，优先写进 `AGENTS.md`、`README.md` 或代码注释，不要直接写进页面正文

## 部署与验证

### 本地命令

```bash
npm install
npm run dev
npm run check
npm run build
npm run preview
```

### 发布

- 推送 `main` 会触发 `.github/workflows/deploy.yml`
- GitHub Pages 自动发布 `dist/`

### 常用检查

```bash
gh run list --limit 5
gh run watch <run_id> --exit-status
```

### 页面改动验收

只要改动涉及任意页面、组件、样式或前端交互，就必须做真实浏览器验收；不能只靠代码阅读、静态截图或构建通过来判断完成。

这里的“改动涉及页面”默认指页面代码层改动，例如页面组件、布局、样式、脚本状态、前端交互或页面结构本身发生变化。**如果只是修改 `src/content/blog/` 下的博客正文内容，而没有改页面代码，那么默认不要求浏览器验收；只有用户明确要求看页面效果、截图或做视觉检查时，才补浏览器验收。**

当前本地环境已确认可用的链路：

- `google-chrome`
- `npx playwright`
- 仓库内已有 `puppeteer-core`

如果要用 Chrome / Playwright / Puppeteer 打开外网站点或抓网页截图，先检查当前 shell 里是否存在 `http_proxy`、`https_proxy` 或 `all_proxy`。如果存在，必须把同一代理显式传给浏览器，例如：

```bash
google-chrome --proxy-server="$https_proxy" https://example.com
```

或在 Puppeteer / Playwright 的启动参数里补：

```text
--proxy-server=<proxy_url>
```

不要假设 shell 能联网时，Chrome 也会自动走同一条代理链路。

推荐按下面的顺序执行：

1. 启动本地站点，固定端口，避免每次验收地址漂移：

```bash
npm run dev -- --host 127.0.0.1 --port 4321
```

2. 用 Chrome 或其他真实浏览器直接打开要验收的页面。

```bash
google-chrome http://127.0.0.1:4321/
google-chrome http://127.0.0.1:4321/old-blog/
```

3. 站点启用了 `trailingSlash: always`，所以除了首页以外，路径要带结尾 `/`；例如应打开 `/old-blog/`，不要打开 `/old-blog`，否则会直接落到 404。

4. 每个改过的页面至少检查两轮：

- 桌面宽度先看一遍，检查首屏、标题层级、正文区、按钮、标签、表单、页脚和主视觉是否成立
- 再切到移动宽度看一遍，确认没有横向滚动、内容截断、元素遮挡、点击区域失效或间距崩坏

5. 如果页面有交互，必须把核心交互完整走一遍，再根据真实视觉效果判断是否达标。不要只打开页面不点。

6. 如果任务包含网页截图、文章配图或验收截图，默认使用接近正常显示器的横向比例，例如 `16:10` 或 `16:9`，推荐尺寸如 `1440x900`、`1600x900`。不要默认使用超长整页截图；除非“长页面本身”就是需要说明的对象，否则应截取关键视口，必要时拆成多张图。

7. `old-blog` 的最低验收动作按这组步骤走：

- 打开 `/old-blog/`
- 确认趋势图、分类区、检索区和文章列表都已经渲染出来
- 在检索框输入 `treevalue`
- 点击一个分类 chip；当前仓库可以直接点第一个 `Bzoj`
- 观察筛选结果数量和结果列表是否随交互变化
- 当前本地实测基线可参考：初始是 `315` 篇，输入 `treevalue` 后是 `4` 篇，再点 `Bzoj` 后会变成 `0` 篇，点击“重置筛选”后恢复到 `315` 篇；如果以后旧站数据更新，允许数值变化，但交互链路本身必须跑通
- 点击“重置筛选”，确认列表恢复到初始状态
- 检查标题跳转、时间、阅读量、评论数是否还在

8. 如果当前环境只能跑 headless Chrome、自动脚本或截图探测，那只能算预检，不算最终验收；必须切换到能看见真实浏览器窗口的环境，肉眼确认后，才能继续 `commit` 或 `push`。

### 提交前最低要求

默认至少执行：

```bash
npm run build
```

如果改动涉及任意页面代码：

- 必须先完整执行上面的“页面改动验收”
- 验收完成后再执行 `npm run build`
- 通过后才能 `commit` 并 `push`

如果只是修改博客正文内容，且没有改页面代码：

- 默认执行 `npm run build` 即可
- 除非用户明确要求，否则不需要额外做浏览器视觉验收

如果改动涉及 `old-blog`：

- 检查筛选器能否正常工作
- 检查年份趋势图是否正常
- 检查标题跳转、时间、阅读量显示是否还在
- 检查没有残留本地临时端口或测试文件

## 当前已知仓库特性

- 当前 `src/content/blog/`、`src/content/routes/`、`src/content/projects/` 可以为空
- 即使内容目录为空，页面也必须正常渲染空状态
- 旧站筛选器已迁到 Vue；当前不需要引入更重的前端框架
- `siteUrl`、仓库地址、导航等关键信息集中在 `src/config/site.ts`

## 如果未来要改域名或仓库名

至少同步检查：

- `astro.config.mjs`
- `src/config/site.ts`
- GitHub Pages 仓库设置
- `README.md`

## 联系入口

当前默认联系入口：

- 邮箱：`hansbug@buaa.edu.cn`
- GitHub Issue
- GitHub Discussions

任何改动都不要把联系入口藏得太深，至少首页、联系页、页脚应可找到。

## 推荐的阅读顺序

新协作者或 agent 建议按这个顺序建立上下文：

1. `README.md`
2. `AGENTS.md`
3. `src/content.config.ts`
4. `src/config/site.ts`
5. `src/utils/content.ts`
6. 目标页面与对应组件

如果只是发新文章，通常不需要通读全站实现。

如果只是改 `old-blog` 体验，优先从：

1. `src/pages/old-blog.astro`
2. `src/components/OldBlogExplorer.vue`
3. `src/components/OldBlogFilterSelect.vue`
4. `src/styles/global.css`

开始。
